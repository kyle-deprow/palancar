interface:
  display_name: "G2 SDK Bridge"
  short_description: "Use the current EvenAppBridge API safely"
  default_prompt: "Use $g2-sdk-bridge to integrate the current Even Hub SDK bridge into Palancar."
